<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 配置别名 -->
	<typeAliases>
		<package name="com.lanjerry.ssm.po"/>
	</typeAliases>
	
	<!-- 配置mapper，由于使用spring和mybatis的整合包进行扫描，这里不需要配置（mapper.xml和mapper.java必须在同个目录且同名） -->
</configuration>